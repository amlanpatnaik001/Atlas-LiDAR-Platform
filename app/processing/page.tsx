"use client"; import { CheckCircle2, Circle, Loader2 } from "lucide-react"; import { Panel } from "../../components/ui/Panel"; import { Badge } from "../../components/ui/Badge";
export default function Processing(){const steps=[['Upload','Source file received','done'],['Validate','Integrity + CRS checks','done'],['Clean','Noise / outlier filtering','done'],['Classify','Ground and feature classes','active'],['Optimize','Tile + prepare web format','pending'],['Publish','Viewer and exports ready','pending']];return <div className="space-y-6"><div><h1 className="text-3xl font-semibold">Processing Center</h1><p className="mt-1 text-sm text-slate-500">Track the journey from raw point cloud to publishable dataset.</p></div><Panel title="AT-2026-024 · Route 101 South" subtitle="Prototype processing job" action={<Badge tone="blue">Classifying</Badge>}><div className="space-y-3">{steps.map(([t,d,s],i)=><div key={t} className="flex items-center gap-4 rounded-xl border border-slate-800/70 bg-slate-900/30 p-4"><div className="shrink-0">{s==='done'?<CheckCircle2 className="text-emerald-400" size={20}/>:s==='active'?<Loader2 className="animate-spin text-blue-400" size={20}/>:<Circle className="text-slate-600" size={20}/>}</div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><div className="font-medium">{t}</div><div className="text-xs text-slate-500">{s==='done'?'Complete':s==='active'?'In progress':'Queued'}</div></div><p className="mt-1 text-xs text-slate-500">{d}</p>{s==='active'&&<div className="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-800"><div className="h-full w-[58%] rounded-full bg-blue-500"/></div>}</div><div className="hidden text-xs text-slate-600 sm:block">0{i+1}</div></div>)}</div></Panel></div>}
